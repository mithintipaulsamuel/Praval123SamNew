import { DirectiveEx1Directive } from './directive-ex1.directive';

describe('DirectiveEx1Directive', () => {
  it('should create an instance', () => {
    const directive = new DirectiveEx1Directive();
    expect(directive).toBeTruthy();
  });
});
