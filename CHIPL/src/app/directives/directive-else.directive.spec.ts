import { DirectiveElseDirective } from './directive-else.directive';

describe('DirectiveElseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveElseDirective();
    expect(directive).toBeTruthy();
  });
});
